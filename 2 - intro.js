// globals - no window
// _dirname - path to current directory
// _file name - file name
// require - function to use modules (commonJS)// module - info about currenbt module (file)
// process - info abount env wheere the program is being executed 
setInterval(() => {
    console.log('evey 1000 ms')
}, 1000);